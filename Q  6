#include <stdio.h>
int main()
{
	int n,i,sum=0;
	printf("enter the number:");
	scanf("%d",&n);
	for(i=0;i<=n;i=i++);
	{
		sum=sum+i;
		
	}
	printf("the sum of even numbers till %d is ; %d",n,sum);
	return 0;
	
	
}
